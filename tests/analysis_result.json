{"dryRun":false,"functions":[{"edges":[{"from":"prod","to":"ai"},{"from":"prod","to":"bi"}],"name":"dot_product","nodes":[{"col":11,"dependencyCount":0,"deps":[],"depth":0,"hasDivision":false,"isAccumulator":true,"isSafe":false,"line":15,"name":"sum","type":"float"},{"col":15,"dependencyCount":0,"deps":[],"depth":0,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":17,"name":"ai","type":"float"},{"col":15,"dependencyCount":0,"deps":[],"depth":0,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":18,"name":"bi","type":"float"},{"col":15,"dependencyCount":2,"deps":["ai","bi"],"depth":1,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":19,"name":"prod","type":"float"}],"safeTodemote":3,"totalFloatVars":4},{"edges":[],"name":"relu","nodes":[{"col":11,"dependencyCount":0,"deps":[],"depth":0,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":27,"name":"result","type":"float"}],"safeTodemote":1,"totalFloatVars":1},{"edges":[{"from":"inv","to":"half"}],"name":"sigmoid_approx","nodes":[{"col":11,"dependencyCount":0,"deps":[],"depth":1,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":33,"name":"half","type":"float"},{"col":11,"dependencyCount":1,"deps":["half"],"depth":3,"hasDivision":true,"isAccumulator":false,"isSafe":false,"line":34,"name":"inv","type":"float"}],"safeTodemote":1,"totalFloatVars":2},{"edges":[{"from":"vi2","to":"vi"}],"name":"l2_norm_sq","nodes":[{"col":11,"dependencyCount":0,"deps":[],"depth":0,"hasDivision":false,"isAccumulator":true,"isSafe":false,"line":40,"name":"acc","type":"float"},{"col":15,"dependencyCount":0,"deps":[],"depth":0,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":42,"name":"vi","type":"float"},{"col":15,"dependencyCount":1,"deps":["vi"],"depth":1,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":43,"name":"vi2","type":"float"}],"safeTodemote":2,"totalFloatVars":3},{"edges":[{"from":"sx","to":"x"},{"from":"res","to":"sx"}],"name":"scale_bias","nodes":[{"col":15,"dependencyCount":0,"deps":[],"depth":0,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":52,"name":"x","type":"float"},{"col":15,"dependencyCount":1,"deps":["x"],"depth":1,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":53,"name":"sx","type":"float"},{"col":15,"dependencyCount":1,"deps":["sx"],"depth":2,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":54,"name":"res","type":"float"}],"safeTodemote":3,"totalFloatVars":3},{"edges":[{"from":"t2","to":"t1"},{"from":"t3","to":"t2"},{"from":"t4","to":"t3"},{"from":"t5","to":"t4"}],"name":"deep_chain","nodes":[{"col":11,"dependencyCount":0,"deps":[],"depth":1,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":61,"name":"t1","type":"float"},{"col":11,"dependencyCount":1,"deps":["t1"],"depth":2,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":62,"name":"t2","type":"float"},{"col":11,"dependencyCount":1,"deps":["t2"],"depth":3,"hasDivision":false,"isAccumulator":false,"isSafe":true,"line":63,"name":"t3","type":"float"},{"col":11,"dependencyCount":1,"deps":["t3"],"depth":4,"hasDivision":false,"isAccumulator":false,"isSafe":false,"line":64,"name":"t4","type":"float"},{"col":11,"dependencyCount":1,"deps":["t4"],"depth":5,"hasDivision":false,"isAccumulator":false,"isSafe":false,"line":65,"name":"t5","type":"float"}],"safeTodemote":3,"totalFloatVars":5}],"originalSource":"/**\n * test_kernels.cpp\n * Sample numerical kernels for testing the Precision-Aware Type Demotion Tool.\n *\n * When analyzed:\n *  - ai, bi, prod          → demoted to __fp16  (depth ≤ 1, no division)\n *  - sum                   → kept at float      (accumulation, depth grows)\n *  - result (relu)         → demoted to __fp16  (depth 1, no division)\n *  - inv (sigmoid)         → kept at float      (has division)\n */\n#include <cstddef>\n\n// ── 1. Dot product ────────────────────────────────────────────────────────────\nfloat dot_product(float* a, float* b, int n) {\n    float sum = 0.0f;                  // accumulates — kept\n    for (int i = 0; i < n; i++) {\n        float ai   = a[i];             // depth 0 — SAFE\n        float bi   = b[i];             // depth 0 — SAFE\n        float prod = ai * bi;          // depth 1 — SAFE\n        sum += prod;\n    }\n    return sum;\n}\n\n// ── 2. ReLU activation ────────────────────────────────────────────────────────\nfloat relu(float x) {\n    float result = x > 0.0f ? x : 0.0f;   // depth 1 — SAFE\n    return result;\n}\n\n// ── 3. Sigmoid approximation (contains division — blocked) ───────────────────\nfloat sigmoid_approx(float x) {\n    float half  = x * 0.5f;           // depth 1 — SAFE\n    float inv   = 1.0f / (1.0f + half); // division — BLOCKED\n    return inv;\n}\n\n// ── 4. L2 norm squared ───────────────────────────────────────────────────────\nfloat l2_norm_sq(float* v, int n) {\n    float acc = 0.0f;                  // accumulates — kept\n    for (int i = 0; i < n; i++) {\n        float vi  = v[i];              // depth 0 — SAFE\n        float vi2 = vi * vi;           // depth 1 — SAFE\n        acc += vi2;\n    }\n    return acc;\n}\n\n// ── 5. Element-wise scale + bias ─────────────────────────────────────────────\nvoid scale_bias(float* out, const float* in, float scale, float bias, int n) {\n    for (int i = 0; i < n; i++) {\n        float x   = in[i];             // depth 0 — SAFE\n        float sx  = x * scale;         // depth 1 — SAFE\n        float res = sx + bias;         // depth 2 — SAFE\n        out[i]    = res;\n    }\n}\n\n// ── 6. Deep chain (should NOT be demoted) ────────────────────────────────────\nfloat deep_chain(float a, float b, float c, float d) {\n    float t1 = a + b;      // depth 1 — SAFE\n    float t2 = t1 * c;     // depth 2 — SAFE\n    float t3 = t2 - d;     // depth 3 — SAFE (borderline)\n    float t4 = t3 * t3;    // depth 4 — BLOCKED (depth > 3)\n    float t5 = t4 + 1.0f;  // depth 5 — BLOCKED\n    return t5;\n}\n","rewrittenSource":"/**\n * test_kernels.cpp\n * Sample numerical kernels for testing the Precision-Aware Type Demotion Tool.\n *\n * When analyzed:\n *  - ai, bi, prod          → demoted to __fp16  (depth ≤ 1, no division)\n *  - sum                   → kept at float      (accumulation, depth grows)\n *  - result (relu)         → demoted to __fp16  (depth 1, no division)\n *  - inv (sigmoid)         → kept at float      (has division)\n */\n#include <cstddef>\n\n// ── 1. Dot product ────────────────────────────────────────────────────────────\nfloat dot_product(float* a, float* b, int n) {\n    float sum = 0.0f;                  // accumulates — kept\n    for (int i = 0; i < n; i++) {\n        __fp16 ai   = a[i];             // depth 0 — SAFE\n        __fp16 bi   = b[i];             // depth 0 — SAFE\n        __fp16 prod = ai * bi;          // depth 1 — SAFE\n        sum += prod;\n    }\n    return sum;\n}\n\n// ── 2. ReLU activation ────────────────────────────────────────────────────────\nfloat relu(float x) {\n    __fp16 result = x > 0.0f ? x : 0.0f;   // depth 1 — SAFE\n    return result;\n}\n\n// ── 3. Sigmoid approximation (contains division — blocked) ───────────────────\nfloat sigmoid_approx(float x) {\n    __fp16 half  = x * 0.5f;           // depth 1 — SAFE\n    float inv   = 1.0f / (1.0f + half); // division — BLOCKED\n    return inv;\n}\n\n// ── 4. L2 norm squared ───────────────────────────────────────────────────────\nfloat l2_norm_sq(float* v, int n) {\n    float acc = 0.0f;                  // accumulates — kept\n    for (int i = 0; i < n; i++) {\n        __fp16 vi  = v[i];              // depth 0 — SAFE\n        __fp16 vi2 = vi * vi;           // depth 1 — SAFE\n        acc += vi2;\n    }\n    return acc;\n}\n\n// ── 5. Element-wise scale + bias ─────────────────────────────────────────────\nvoid scale_bias(float* out, const float* in, float scale, float bias, int n) {\n    for (int i = 0; i < n; i++) {\n        __fp16 x   = in[i];             // depth 0 — SAFE\n        __fp16 sx  = x * scale;         // depth 1 — SAFE\n        __fp16 res = sx + bias;         // depth 2 — SAFE\n        out[i]    = res;\n    }\n}\n\n// ── 6. Deep chain (should NOT be demoted) ────────────────────────────────────\nfloat deep_chain(float a, float b, float c, float d) {\n    __fp16 t1 = a + b;      // depth 1 — SAFE\n    __fp16 t2 = t1 * c;     // depth 2 — SAFE\n    __fp16 t3 = t2 - d;     // depth 3 — SAFE (borderline)\n    float t4 = t3 * t3;    // depth 4 — BLOCKED (depth > 3)\n    float t5 = t4 + 1.0f;  // depth 5 — BLOCKED\n    return t5;\n}\n"}
